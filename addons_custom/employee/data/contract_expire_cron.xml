<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="ir_cron_expire_contracts" model="ir.cron">
        <field name="name">Tự động chuyển hợp đồng quá hạn sang 'Hết hạn'</field>
        <field name="model_id" ref="model_contract"/>
        <field name="state">code</field>
        <field name="code">model._cron_expire_contracts()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active">True</field>
    </record>
</odoo>