<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="employee.ContractDashboard">
        <Layout display="display" className="'o_dashboard h-100'">
            <t t-set-slot="control-panel-top-left">
                <button class="btn btn-primary me-2" t-on-click="openCustomers">
                    Customers
                </button>
                <button class="btn btn-primary me-2" t-on-click="openLeads">
                    Leads
                </button>
                <button class="btn btn-secondary" t-on-click="openSettings" title="Tùy chỉnh Dashboard">
                    <i class="fa fa-cog"/>
                </button>
            </t>

            <div class="p-4">
                <t t-if="statistics.new_contracts !== undefined">
                    <t t-foreach="items" t-as="item" t-key="item.id">
                        <DashboardItem size="item.size || 1">
                            <t t-set="itemProps"
                               t-value="item.props ? item.props(statistics) : {data: statistics}"/>
                            <t t-component="item.Component" t-props="itemProps"/>
                        </DashboardItem>
                    </t>
                </t>
                <t t-else="">
                    <p>Đang tải dữ liệu...</p>
                </t>
            </div>
        </Layout>
    </t>

</templates>